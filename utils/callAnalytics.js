/**
 * emitCallAnalytics
 * -----------------
 * ⚠️ TEMP DISABLED FOR DB SAFETY
 *
 * Reason:
 * - DB persistence is handled ONLY by sendAnalyticsEvent()
 * - This function is kept for future realtime / metrics / alerts
 * - DO NOT add DB or API calls here
 */
export function emitCallAnalytics(eventType, payload) {
  // 🔕 intentionally disabled (no-op)
  return;
}
